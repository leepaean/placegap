# Release Policy

`v0.0.x` releases are engineering and methodology bootstrap increments.

`v0.1.0` is reserved for the first usable diagnostic prototype that can complete the validated chain:

`Evidence → Finding → Human Verify → Gap/Hypothesis → Contradiction/Unknown → Export`

A release tag should represent something a new user can run or inspect, not an arbitrary calendar milestone.
