# Source Pack Template

A validation source pack should make the evidence boundary explicit.

Recommended structure:

```text
sources/
manifest.json
evidence.json
notes.md
```

`manifest.json` should record which materials were provided to all comparison conditions so ordinary GPT, expert-prompt GPT, and PlaceGap receive the same evidence base.
