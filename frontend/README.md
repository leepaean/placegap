# Frontend

The v0.1 frontend will be a React + TypeScript + Vite workbench.

Do not implement a dashboard first.

UI priority:

1. Evidence Board
2. Human Finding Verification
3. Diagnostic Map
4. Hypothesis Inspector

The repository bootstrap intentionally leaves frontend code empty until the domain model survives its first tests.
